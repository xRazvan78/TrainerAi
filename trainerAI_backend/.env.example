# Optional explicit override. If set, this value is used directly.
DATABASE_URL=postgresql://user:parola@host:port/nume_baza_de_date

# PostgreSQL settings (credentials intentionally left empty).
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=trainerAI_database
